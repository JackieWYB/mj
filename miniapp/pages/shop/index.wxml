<view class="page">
  <view class="card" wx:for="{{items}}" wx:key="id">
    <view>{{item.name}} - {{item.price}}</view>
    <button class="btn-primary" data-id="{{item.id}}" bindtap="buy">购买</button>
  </view>
  <empty-state wx:if="{{!items.length}}" text="暂无商品" />
</view>
