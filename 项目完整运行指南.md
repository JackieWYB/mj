# 卡五星项目《完整运行指南》

> 适用范围：当前仓库的 **monorepo 最小可运行骨架**（`packages/server` / `packages/miniprogram` / `packages/game`）以及现有文档与规则引擎测试。

---

## 1. 你将运行的内容

本仓库当前可运行部分分为 4 类：

1. **后端最小服务**：`packages/server`
   - NestJS + ws
   - 提供 HTTP：`/auth/login/mock`、`/room/create`、`/room/join`
   - 提供 WS：`/ws`，支持 `AUTH/JOIN_ROOM/READY/END_GAME/PING`

2. **小程序大厅最小示例**：`packages/miniprogram`
   - 可在微信开发者工具直接打开运行
   - 使用本地 mock API，不依赖云服务

3. **游戏客户端脚本骨架**：`packages/game`
   - Cocos Creator TS 脚本
   - 可连接 WS 并打印消息日志

4. **规则引擎测试**：`packages/rule-engine`
   - 使用根脚本 `npm test` 可编译并运行单元测试

---

## 2. 环境准备

## 2.1 必备软件
- Node.js 18+（建议 20 LTS）
- npm 9+
- 微信开发者工具（运行小程序）
- Cocos Creator（运行游戏客户端，建议与团队统一版本）

## 2.2 拉代码并进入目录
```bash
git clone <your-repo-url>
cd mj
```

---

## 3. 配置环境变量

仓库根目录已提供 `.env.example`。

### 3.1 创建本地配置
```bash
cp .env.example .env
```

### 3.2 关键字段说明
- `SERVER_PORT`：后端 HTTP 端口（默认 3000）
- `SERVER_WS_PATH`：WS 路径（默认 `/ws`）
- `MINIPROGRAM_API_BASE`：小程序 API 地址（本地通常 `http://localhost:3000`）
- `GAME_WS_URL`：Cocos 客户端 WS 地址（本地通常 `ws://localhost:3000/ws`）
- 其余字段当前主要用于占位/后续扩展（如 `JWT_SECRET`、`ROOM_MAX_PLAYERS`）

---

## 4. 安装依赖

在仓库根目录执行：
```bash
npm install
```

> 若你处于离线/内网环境，请先配置 npm 私有镜像。

---

## 5. 启动后端（packages/server）

### 5.1 开发模式（推荐）
```bash
npm run server:dev
```

看到如下日志表示成功：
- `[server] listening http://localhost:3000`

### 5.2 验证 HTTP 接口
新开终端执行：
```bash
curl -X POST http://localhost:3000/auth/login/mock -H 'Content-Type: application/json' -d '{}'
```
你将得到类似：
```json
{"uid":"Uxxxx","token":"mock-Uxxxx"}
```

创建房间：
```bash
curl -X POST http://localhost:3000/room/create -H 'Content-Type: application/json' -d '{"uid":"U1001"}'
```

加入房间：
```bash
curl -X POST http://localhost:3000/room/join -H 'Content-Type: application/json' -d '{"roomId":"Rxxxx","uid":"U1002"}'
```

---

## 6. 启动小程序（packages/miniprogram）

1. 打开微信开发者工具
2. 选择“导入项目”
3. 项目目录选择：`packages/miniprogram`
4. `AppID` 可先用测试号/无 AppID 调试模式
5. 编译运行后，在页面点击：
   - 登录(mock)
   - 创建房间
   - 加入房间（示例提示）

> 当前此包使用本地 mock，主要用于页面联调与流程演示。

---

## 7. 启动 Cocos 客户端脚本（packages/game）

### 7.1 导入脚本
将 `packages/game/assets/scripts` 复制到你的 Cocos 项目 `assets/scripts` 下。

### 7.2 在场景入口调用
在场景初始化脚本中调用：
```ts
import { bootstrapDemo } from './scripts/Bootstrap';

bootstrapDemo();
```

### 7.3 设置全局参数（可选）
运行前可注入：
- `GAME_WS_URL=ws://localhost:3000/ws`
- `GAME_TOKEN=mock-U1001`
- `GAME_ROOM_ID=Rxxxx`

### 7.4 运行效果
- 连接成功打印 `NET_OPEN`
- 自动发送 `AUTH`
- 收到 `AUTH_OK`
- 若有 `GAME_ROOM_ID`，自动发送 `JOIN_ROOM`

---

## 8. 跑规则引擎测试（packages/rule-engine）

在根目录执行：
```bash
npm test
```

该命令会：
1. `tsc -p tsconfig.build.json` 编译测试目标
2. `node --test .tmp-test/test/engine.test.js` 执行单测

---

## 9. 推荐联调顺序（一次跑通）

1. 根目录 `npm install`
2. `cp .env.example .env`
3. 终端A：`npm run server:dev`
4. 终端B：调用 `/auth/login/mock`、`/room/create`
5. 微信开发者工具打开 `packages/miniprogram`，验证大厅流程
6. Cocos 工程加载 `packages/game/assets/scripts`，验证 ws 连通
7. 根目录 `npm test`，验证规则引擎测试通过

---

## 10. 常见问题排查

### Q1: `npm install` 失败（403/网络问题）
- 检查 npm registry 是否可访问
- 切换镜像后重试

### Q2: server 无法启动
- 检查 `SERVER_PORT` 是否被占用
- 检查 Node 版本是否 >= 18

### Q3: WS 连不上
- 确认 `GAME_WS_URL` 与 `SERVER_WS_PATH` 一致
- 确认后端正在运行（3000端口可访问）

### Q4: 小程序无法编译
- 检查导入目录是否为 `packages/miniprogram`
- 检查开发者工具基础库设置

---

## 11. 当前状态说明
- 该仓库是“最小可运行骨架 + 完整设计文档”
- 功能更偏向联调框架与架构演示，不是生产完成版
- 下一步建议：
  1. 将 `packages/server` 的内存状态切到 Redis/MySQL
  2. 将 `packages/miniprogram` 从 mock API 切到真实后端
  3. 将 `packages/game` 脚本挂接到完整 Cocos UI/动画资源

