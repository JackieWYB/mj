{
  "name": "mj-rule-engine",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build:test": "tsc -p tsconfig.build.json",
    "test": "npm run build:test && node --test .tmp-test/test/engine.test.js"
  }
}
